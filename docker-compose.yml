version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: community-feed-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/db.sqlite3:/app/db.sqlite3
    restart: always